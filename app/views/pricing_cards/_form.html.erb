<%= form_with(model: pricing_card) do |form| %>
  <% if pricing_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pricing_card.errors.count, "error") %> prohibited this pricing_card from being saved:</h2>

      <ul>
        <% pricing_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :pricing_card_title, style: "display: block" %>
    <%= form.text_field :pricing_card_title %>
  </div>

  <div>
    <%= form.label :pricing_card_description, style: "display: block" %>
    <%= form.text_area :pricing_card_description %>
  </div>

  <div>
    <%= form.label :pricing_card_image_url, style: "display: block" %>
    <%= form.text_field :pricing_card_image_url %>
  </div>

  <div>
    <%= form.label :pricing_card_total_cost, style: "display: block" %>
    <%= form.number_field :pricing_card_total_cost %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
