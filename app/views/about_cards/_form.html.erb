<%= form_with(model: about_card) do |form| %>
  <% if about_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(about_card.errors.count, "error") %> prohibited this about_card from being saved:</h2>

      <ul>
        <% about_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :about_card_title, style: "display: block" %>
    <%= form.text_field :about_card_title %>
  </div>

  <div>
    <%= form.label :about_card_description, style: "display: block" %>
    <%= form.text_area :about_card_description %>
  </div>

  <div>
    <%= form.label :about_card_image_url, style: "display: block" %>
    <%= form.text_field :about_card_image_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
